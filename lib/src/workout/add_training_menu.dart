import 'package:flutter/material.dart';

class AddPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('トレーニングメニュー追加'),
      ),
      body: Container(
        child: Text('ここにメニューを追加する'),
      ),
    );
  }
}
